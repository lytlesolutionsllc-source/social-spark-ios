name: iOS Build

on:
  push:
    branches: ["main", "mvp-checkpoint-**", "feature/**", "bugfix/**"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Show Xcode version
        run: xcodebuild -version

      - name: Show project and schemes
        run: |
          ls -la
          xcodebuild -list -project SocialSpark.xcodeproj

      - name: Build SocialSpark (generic iOS Simulator)
        run: |
          set -o pipefail
          xcodebuild \
            -project SocialSpark.xcodeproj \
            -scheme SocialSpark \
            -configuration Debug \
            -destination 'generic/platform=iOS Simulator' \
            clean build
